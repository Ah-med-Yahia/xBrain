import 'package:flutter/material.dart';

class ProfilePicturePageView extends StatelessWidget {
  const ProfilePicturePageView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
